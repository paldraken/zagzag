%html
  %head
    %title Привет
  %body
    %h1 Hello haml!!!!!
    %p.long-text many many texts!!!
    %div.barrel ha ha ha
    %p.big_text Like the Basic Use example, this example will start a static web server at http://localhost:9001/, with its base path set to the www-root directory relative to the gruntfile. Unlike the other example, this is done by creating a brand new task. in fact, this plugin isn't even installed!   

    =render_patrial 'Modules/module_one.haml'
